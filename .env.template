MONGODB_URL=
DB_NAME=
PORT=8000

# Optional SDK default when createClient() is called without baseUrl.
REQUESTING_API_BASE_URL=http://localhost:8000/api

# Bcrypt cost factor for password hashing (default 12).
BCRYPT_ROUNDS=12
